(function() {
    var app = angular.module('app');
    var controllerId = 'app.views.authors.index';

    app.controller(controllerId,
    [
        '$scope',
        function($scope){
            var vm = this;
        }
    ]);
});